export interface DownloadDocumentPropsType {
  title: string;
  file: {
    image_id: any;
    name: any;
    suffix: any;
  };
}

export interface StatusType {
  img: any;
  loading: boolean;
  error: boolean;
}
